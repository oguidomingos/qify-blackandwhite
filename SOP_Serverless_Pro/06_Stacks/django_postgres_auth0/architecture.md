# Django + PostgreSQL + Auth0

- Monolito modular em Django; DRF para API.
- Auth0 (OIDC), RBAC via claims, integração com Stripe nativo se necessário.
- PostgreSQL com índices compostos e migrações seguras.
